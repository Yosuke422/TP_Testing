<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Machine Selection</title>
</head>
<body>
    <h1>Select Machines for <%= selectedGym %></h1>
    <form action="/selectMachines" method="post">
        <% machines.forEach(machine => { %>
            <input type="checkbox" id="<%= machine %>" name="machines" value="<%= machine %>">
            <label for="<%= machine %>"><%= machine %> (Available)</label><br>
        <% }) %>
        <button type="submit">Confirm Selection</button>
    </form>
</body>
</html>
